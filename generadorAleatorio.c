#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <math.h>

using namespace std;

int main(){
    FILE *Gen=NULL;
    Gen=fopen("GastosYIngresos.txt", "w");


     if (Ing==NULL){
        printf("No se puede abrir el archivo");
        return -1;
    }

    int menor = 45, numero;
    srand(time(NULL));

    int cont=1;

    for (int i=0; i<10; i++){
        numero = rand() % 45;
        count << numero ",";
        
    }
    _getch();



}